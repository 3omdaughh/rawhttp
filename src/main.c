#include<stdio.h>

#include "rawhttp_/error.h"

signed main(int argc, char** argv)
{
    (void)argc;
    (void)argv;
    
    LOG_INFO("[*] rawhttp starting up (still under construction no network yet)");
    printf("rawhttp: raw socket HTTP client (work in progress)\n");

    return 0;
} 
